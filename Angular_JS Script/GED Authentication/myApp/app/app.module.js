(function () {
    'use strict';
    
    var requires = [
        "ngRoute",
    ];
    
    angular.module('app', requires);

    angular.module('app').controller('App', function($scope, $http){
       
    });
})();

